**Review:**

There are syntax errors in the given SQL statement:

1. The `select` statement must specify what columns to select (e.g., `*` for all columns, or column names).
2. The `from` clause is missing the column list after the `select` keyword.

**Corrected Code:**

```sql
SELECT * FROM Customer WHERE CustomerID > 30;
```

**Explanation:**
- `SELECT *` selects all columns from the `Customer` table.
- The rest remains the same, but now uses proper SQL syntax.